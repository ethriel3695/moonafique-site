{
  "name": "moonafique-workspace",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=moonafique-site",
    "lint": "turbo run lint",
    "start": "turbo run start --filter=moonafique-site",
    "storybook": "turbo run storybook --filter=moonafique-site",
    "build-storybook": "turbo run build-storybook",
    "test": "turbo run test",
    "turbo": "turbo"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
